---
interface Props {
  title: string;
  description: string;
  icon: string;
  ctaText?: string;
  ctaLink?: string;
}

const { title, description, icon, ctaText = "Detaylı Bilgi →", ctaLink = "/iletisim" } = Astro.props;
---

<a href={ctaLink} class="group block h-full">
  <div class="h-full bg-white rounded-xl p-8 shadow-[0_4px_20px_rgba(0,0,0,0.05)] border border-gray-100 transition-all duration-300 hover:shadow-xl hover:-translate-y-2 flex flex-col items-center">
    
    <div class="mb-5 text-primary text-5xl group-hover:scale-110 transition-transform duration-300">
      <i class={icon}></i>
    </div>

    <h3 class="text-xl font-bold text-[#1C1C1C] mb-4 text-center group-hover:text-primary transition-colors">
      {title}
    </h3>

    <p class="text-[#333333] font-normal leading-relaxed text-justify w-full mb-6">
      {description}
    </p>

    <div class="mt-auto pt-4 border-t border-gray-50 w-full flex justify-center">
      <span class="inline-flex items-center gap-2 text-sm font-bold text-primary bg-primary/5 px-4 py-2 rounded-full group-hover:bg-primary group-hover:text-white transition-all">
        {ctaText} <i class="fa-solid fa-arrow-right"></i>
      </span>
    </div>
    
  </div>
</a>