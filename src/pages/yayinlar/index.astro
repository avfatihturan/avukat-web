---
import { getCollection } from 'astro:content';
import BaseLayout from '../../layouts/BaseLayout.astro';
import Hero from '../../components/Hero.astro';
import ArticleCard from '../../components/ArticleCard.astro';

// Taslak olmayan makaleleri çek
const articles = await getCollection('articles', ({ data }) => !data.draft);

// Tarihe göre sırala (Yeniden eskiye)
articles.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

// Benzersiz etiketleri al (Boş olanlara 'Genel' ata)
const allTags = [...new Set(articles.map(article => article.data.tag ?? 'Genel'))];
---

<BaseLayout
  title="Yayınlar | Av. Fatih Turan"
  description="Av. Fatih Turan'ın hukuki makaleleri ve yayınları. Ceza, İş ve Kira Hukuku konularında güncel bilgiler."
  currentPage="yayinlar"
  canonicalUrl="/yayinlar"
>
  <Hero title="Yayınlar" size="sm" />

  <section class="bg-light">
    <div class="container">
      
      <div class="filter-bar">
        <button class="filter-btn active" data-filter="all">
          Tümü
          <span class="filter-count">{articles.length}</span>
        </button>
        {allTags.map((tag) => (
          <button class="filter-btn" data-filter={tag}>
            {tag}
            <span class="filter-count">
              {articles.filter(a => (a.data.tag ?? 'Genel') === tag).length}
            </span>
          </button>
        ))}
      </div>

      <div class="articles-grid grid-3">
        {articles.map((article) => (
          <div class="article-item" data-tag={article.data.tag ?? 'Genel'}>
            <ArticleCard
              title={article.data.title}
              description={article.data.description}
              href={`/yayinlar/${article.id}`}
              image={article.data.heroImage}
              tag={article.data.tag ?? 'Genel'} 
            />
          </div>
        ))}
      </div>

      <div class="empty-state" style="display: none;">
        <p>Bu kategoride henüz yayın bulunmuyor.</p>
      </div>

    </div>
  </section>
</BaseLayout>

<style>
  /* --- FILTER BAR --- */
  .filter-bar {
    display: flex;
    flex-wrap: wrap;
    gap: 12px;
    margin-bottom: 40px;
    justify-content: center;
  }

  .filter-btn {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    padding: 10px 20px;
    background: var(--card-bg);
    border: 1px solid var(--color-border);
    border-radius: 50px;
    font-family: inherit;
    font-size: 0.85rem;
    font-weight: 600;
    color: var(--color-text);
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .filter-btn:hover {
    border-color: var(--brand-accent);
    color: var(--brand-accent);
  }

  .filter-btn.active {
    background: var(--brand-primary);
    border-color: var(--brand-primary);
    color: #fff;
  }
  :global(html.dark) .filter-btn.active {
    background: var(--brand-accent);
    border-color: var(--brand-accent);
    color: #000;
  }

  .filter-count {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    min-width: 22px;
    height: 22px;
    padding: 0 6px;
    background: rgba(0, 0, 0, 0.1);
    border-radius: 50px;
    font-size: 0.75rem;
    font-weight: 700;
  }

  .filter-btn.active .filter-count { background: rgba(255, 255, 255, 0.2); }
  :global(html.dark) .filter-btn.active .filter-count { background: rgba(0, 0, 0, 0.2); }

  /* --- ARTICLE ITEMS --- */
  .article-item {
    opacity: 1;
    transform: scale(1);
    transition: opacity 0.3s ease, transform 0.3s ease;
  }

  .article-item.hidden { display: none; }

  .empty-state {
    text-align: center;
    padding: 60px 20px;
    color: var(--color-text-secondary);
  }

  @media (max-width: 768px) {
    .filter-bar { gap: 8px; }
    .filter-btn { padding: 8px 16px; font-size: 0.8rem; }
  }
</style>

<script is:inline>
  function initFilter() {
    const filterBtns = document.querySelectorAll('.filter-btn');
    const articleItems = document.querySelectorAll('.article-item');
    const emptyState = document.querySelector('.empty-state');

    if (!filterBtns.length || !articleItems.length) return;

    filterBtns.forEach(btn => {
      btn.addEventListener('click', () => {
        const filter = btn.dataset.filter;

        // Buton durumunu güncelle
        filterBtns.forEach(b => b.classList.remove('active'));
        btn.classList.add('active');

        // Makaleleri filtrele
        let visibleCount = 0;
        articleItems.forEach(item => {
          const tag = item.dataset.tag;
          if (filter === 'all' || tag === filter) {
            item.classList.remove('hidden');
            visibleCount++;
          } else {
            item.classList.add('hidden');
          }
        });

        // Boş durum kontrolü
        if (emptyState) {
          emptyState.style.display = visibleCount === 0 ? 'block' : 'none';
        }
      });
    });
  }

  document.addEventListener('DOMContentLoaded', initFilter);
  document.addEventListener('astro:after-swap', initFilter);
</script>